## 후속 보고 — 제가 드린 진단이 틀렸습니다

이 이슈에서 저는 "그 화면들은 v2.17 보다도 오래된 DAT 에서 나온 것 같다"고
말씀드렸습니다. **틀렸습니다.** 올려 주신 `게임검사` 결과는 정상이었고, 파일도
맞는 것을 쓰고 계셨습니다.

진짜 원인은 **v1.2 업데이트가 시나리오·카드 데이터의 수정본을 실행코드 안에
싣고 있고, 게임이 `CULDCEPT.DAT` 이 아니라 그쪽을 읽는다**는 것이었습니다.
그러니 DAT 을 아무리 새로 받아도 그 화면만 원문 그대로였던 게 당연했습니다.

* 시나리오 쪽 — v2.25 에서 실행코드 사본을 한글화(이벤트 85개)
* 카드 쪽 — 실행코드 사본이 **v2.17 무렵 번역으로 굳어 있던 것**을 v2.26 에서
  지금 데이터로 다시 만듦(`니케`·`암드 팔라딘`·`스타추` 등 13곳)

이름 통일(`배트`, `란고르`)은 v2.20 에 들어가 있습니다.

브리드 드래곤 카드 이름만 아직 못 찾았습니다. 그 화면에서 `원문추적.cmd` 를
돌려 주시면 도움이 됩니다(그 파일이 배포본에 빠져 있었습니다 — v2.26 부터
들어갑니다).
